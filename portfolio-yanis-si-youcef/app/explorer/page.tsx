import Link from "next/link";import {documents,collections} from "@/lib/data";
export default function Explorer(){const years=[...new Set(documents.map(d=>d.year))].sort((a,b)=>b-a);const series=[...new Set(documents.map(d=>d.series))].sort();return <section><div className="page-head"><p>Index du fonds</p><h1>Explorer</h1></div><div className="explore-grid"><div><h2>Par collection</h2>{collections.map(c=><Link href={`/?collection=${c.slug}`} key={c.slug}>{c.name}</Link>)}</div><div><h2>Par année</h2>{years.map(y=><Link href={`/?year=${y}`} key={y}>{y}</Link>)}</div><div><h2>Par série</h2>{series.map(s=><span key={s}>{s}</span>)}</div><div><h2>Par rôle</h2>{[...new Set(documents.map(d=>d.role))].map(r=><span key={r}>{r}</span>)}</div></div></section>}
